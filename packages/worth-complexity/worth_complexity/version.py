"""The package version, in one place.

Separate from ``__init__`` so modules inside the package can stamp a published
record with the version that produced it without importing the package root.
"""

__version__ = "0.1.0"
